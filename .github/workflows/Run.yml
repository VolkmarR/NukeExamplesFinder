# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Run --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Run

on: [push]

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run './build.cmd Run'
        run: ./build.cmd Run
        env:
            NukeExamplesFinder_Credentials__GitHubToken: ${{ secrets.NukeExamplesFinder_Credentials__GitHubToken }}
      - uses: actions/upload-artifact@v1
        with:
          name: Directory.md
          path: output/Directory.md
      - uses: actions/upload-artifact@v1
        with:
          name: Repos.json
          path: output/Repos.json
